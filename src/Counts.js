import React from "react";

function Counts({ent}){
var test=2;
 return(
    <div className="flex justify-center">
        <p>Total detected image counts ({ent}).</p>
    </div>
 );
}
export default Counts;